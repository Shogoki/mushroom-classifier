import { Enum2arrayPipe } from './enum2array.pipe';

describe('Enum2arrayPipe', () => {
  it('create an instance', () => {
    const pipe = new Enum2arrayPipe();
    expect(pipe).toBeTruthy();
  });
});
